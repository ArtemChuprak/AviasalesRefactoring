export const selectTickets = (state) => state.tickets.tickets;

export const selectSearchId = (state) => state.tickets.searchId;

export const selectStopFetch = (state) => state.tickets.stopFetch;

export const selectIsSearch = (state) => state.tickets.isSearch;

export const selectFetchStatus500 = (state) => state.tickets.fetchStatus500;

export const selectIsLoading = (state) => state.tickets.isLoading;

export const selectShowTickets = (state) => state.tickets.showTickets;

export const selectToggle = (state) => state.filter.toggle;

export const selectTabs = (state) => state.sort.tabs;